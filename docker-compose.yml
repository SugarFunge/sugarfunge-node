version: "3.2"

services:
  dev:
    container_name: sf-node
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/sf-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/sf-node/.cargo
    volumes:
      - .:/var/www/sf-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/sf-node --dev --ws-external"
